defaultNamespace: longhorn-system
helm:
  repo: https://charts.rancher.io
  chart: longhorn-crd
  version: 110.0.0+up1.11.3         # 109.3.1+up1.11.2 
  releaseName: longhorn-crd
  
  # Important: Wait for CRDs to be fully established before marking success
  waitForJobs: true

targets:
  - clusterSelector:
      matchLabels:
        needs-security: "true"
